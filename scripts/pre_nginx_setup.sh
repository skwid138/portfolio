# Create a Docker network for NGINX to communicate with other project containers
docker network create portfolio_network

# Create a directory to store Nginx configuration files
mkdir -p ~/nginx/conf.d


